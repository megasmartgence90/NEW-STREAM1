name: Boss Sports IPTV Updater

on:
  schedule:
    # Her gün saat 03:00'te otomatik çalışır (UTC)
    - cron: '0 3 * * *'
  workflow_dispatch: # Manuel olarak da çalıştırılabilir

jobs:
  update-m3u:
    runs-on: ubuntu-latest

    steps:
      - name: Depoyu klonla
        uses: actions/checkout@v4

      - name: Python kurulumunu yap
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Gerekli paketleri yükle
        run: |
          pip install requests

      - name: Scripti çalıştır
        run: |
          python boss_sports.py

      - name: Oluşturulan dosyayı kontrol et
        run: |
          ls -lh *.m3u || echo "M3U dosyası bulunamadı"

      - name: Değişiklikleri commit et ve gönder
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add *.m3u || echo "Dosya eklenemedi"
          git commit -m "🔄 M3U dosyası otomatik güncellendi" || echo "Commit yapılacak değişiklik yok"
          git push || echo "Push işlemi başarısız oldu"
